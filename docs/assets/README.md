# 视觉资产 · Visual assets

- `xiaoyi-console.png`：0.3.0 本地 `operations_sandbox` 模式的实际主控台截图。 / Actual 0.3.0 console in local `operations_sandbox` mode.
- `rl-lab.png`：真实五基线 RL 实验室配置界面；数据集标签明确为公开非港口基准。 / Actual five-baseline RL-lab configuration UI; the dataset is visibly labeled as a public non-port benchmark.
- `social-preview.png`：1280×640 的 GitHub 社交预览候选图，由主控台截图裁切。 / 1280×640 GitHub social-preview candidate cropped from the console screenshot.

截图不包含真实港口实绩、凭据、私有主机名或个人身份信息。运营数值来自动态合成沙箱，不得作为生产数据引用。

The screenshots contain no live-port facts, credentials, private hostnames, or personal identity data. Operational values come from the dynamic sandbox and must not be cited as production data.
