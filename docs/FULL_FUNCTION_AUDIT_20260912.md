# 小懿 AI 全功能与联动验收记录

验收时间：2026-09-12（Asia/Kuala_Lumpur）
范围：`http://127.0.0.1:8010/` 本机运行态、前端按钮与弹窗、后端接口、四系统本机适配器和浏览器异步行为。未发布、未提交 Git、未启用生产写权限。

## 真实浏览器验收

- 主导航 6 个视图：智能对话、决策建议、数据分析、训练中心、知识库、任务中心；视图切换后标题、数据和激活状态一致。
- 对话：本地 4B 模型完成一次真实问答，展示证据、来源、质量标签和回答；收藏、历史恢复、更多操作和报告下载均可用。
- 数据分析：今日、7 日、30 日三种时窗均返回不同序列；四个 KPI、图表和侧栏明细随时窗同步。常态、集中到港、设备故障、需量高峰、大风低能见度五种场景均改变同一模拟链路并显示事件回执。
- 模拟决策：调度员和值班长双角色审批、沙箱执行和回滚均产生审计事件，回执明确 `physical_dispatch=false`。
- 任务中心：泊位候选任务在最后一步停留人工确认，取消不会推进；确认后完成。运营日报任务自动执行并生成 100% 步骤结果。
- 训练中心：真实运行 `rl-20260912T115655-8db4a877` 完成 `320/320`，保留测试集评测和六候选证据可见；回到训练中心会重新读取最新运行记录。六张算法卡片在桌面为 3×2，在 375px 窄屏为单列，正文和公式不被裁切。
- 智能联动中心：7/7 能力模块可见；自然语言编排选中 2 项能力，2 项隔离预览完成，6 个步骤全部完成；本机评测可重新运行，反馈进入 `pending_review` 人工审核队列。
- 四系统联动：港口数字孪生、能碳驾驶舱、马六甲沙盘返回本机回执；航行模拟器保持隔离。失败目标可重试，重试后 `1/1` 完成；执行、打开和刷新入口均可用，生产写入保持关闭。
- 接口中心：8 类连接器、详情、健康检查和离线状态可见；未配置地址和凭据时保持待接入，不伪报在线。
- 知识库：专业目录、权威覆盖、来源审计、知识全景四个入口可打开；分类、状态筛选和搜索可用。附件选择进入待审核表单，暂存不直接进入问答索引。
- 系统状态、准入门禁、通知、设置和形象选择均可打开。设置的主题、默认模式、Top-K 保存后可恢复，并已恢复原始偏好（深海、运营、5）；形象已恢复为领航员。
- 375px 窄屏：顶部导航按钮带可访问名称；侧栏可打开系统状态、通知、设置、管理员四个入口；页面无横向溢出阻断主流程。

## 本轮修复

- 修复 FastAPI `Request` 名称冲突、目标系统身份校验和能源深健康检查超时。
- 修复任务步骤在数据源失败时被提前标记完成、训练零值参数被 `or` 覆盖、验证阶段取消被重新写成训练完成。
- 修复 dashboard/energy 乱序响应、时窗刷新重置、顾问并发响应串位、任务重复推进和自动执行切换任务污染。
- 修复历史清空失败仍提示成功、收藏丢失证据元数据、迟到弹窗响应覆写新窗口、联动执行失败冒充成功、取消未受理冒充成功和未回读的模拟回滚冒充完成。
- 修复训练中心缓存旧运行记录、算法/环境候选数量硬编码、任务步骤状态文案错误，以及碳强度单位标签错误。
- 修复训练矩阵卡片布局和窄屏入口可访问性；资源查询版本提升至 `20260912-function-audit-v3`。

## 自动化验证

- Python：整库 `464 passed`（隔离运行库 `/tmp/xiaoyi_function_audit_final.db`）。
- Node：`44 passed`，涵盖异步竞态、任务互斥、历史/收藏、RL 参数、时窗一致性、联动草稿/预览/执行/取消/恢复和安全模糊测试。
- Ruff、Python compileall、三个前端脚本 `node --check`、`git diff --check`：通过。
- `PATH=.venv/bin:<bundled-node>/bin:$PATH make check`：通过（含隐私扫描、发布检查、依赖检查和 `pnpm test`）。
- 本机运行态联动和只读能力证据：[LINKAGE_AUDIT_20260912.md](LINKAGE_AUDIT_20260912.md)、[linked_agent_acceptance_20260912_final_v1.json](../reports/linked_agent_acceptance_20260912_final_v1.json)。

## 边界

当前 8 类生产连接器均未配置，现场数据未验证，`simulation_mode=true`、`live_data_verified=false`、`dispatch_allowed=false`、`production_authority=false`。航行模拟器保持隔离；公开 AIS、公开能源和运营数值只用于可复现模拟与证据演示，不代表现场 KPI 或生产控制结果。
